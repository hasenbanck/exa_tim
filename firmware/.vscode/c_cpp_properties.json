{
    "configurations": [{
        "name": "ARM",
        "includePath": [
            "${workspaceFolder}"
        ],
        "browse": {
            "path": [
                "${workspaceFolder}"
            ]
        },
        "compileCommands": "${workspaceRoot}/compile_commands.json"
    }],
    "version": 4
}