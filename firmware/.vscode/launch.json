{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (J-Link)",
            "cwd": "${workspaceRoot}",
            "executable": "${workspaceRoot}/exa_tim.elf",
            "request": "launch",
            "type": "cortex-debug",
            "device": "STM32L071KZ",
            "svdFile": "${workspaceRoot}/STM32L071.svd",
            "servertype": "jlink",
            "preLaunchTask": "build"
        }
    ]
}